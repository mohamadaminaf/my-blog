        // فرستادن اطلاعات به جدول Posts (دقت کن به P بزرگ)
        const { error } = await _supabase
            .from('Posts') // <--- اینجا را حتما به 'Posts' تغییر بده!
            .insert([{ title: title, content: content }]);
